language: generic

sudo: required

addons:
    apt:
        packages:
            - nodejs

after_success: |
    which node &&
    node --version && 
    cd awesome-ctf-cheatsheet &&
    npm install --global awesome-lint && 
    awesome-lint
