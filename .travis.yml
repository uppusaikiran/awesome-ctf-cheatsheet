language: generic

sudo: required

addons:
    apt:
        packages:
            - nodejs

after_success: |
    which node &&
    node --version && 
    npm install --global awesome-lint
after_success: 
    awesome-lint
